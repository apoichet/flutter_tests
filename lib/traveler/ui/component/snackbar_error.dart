import 'package:flutter/widgets.dart';

class SnackbarError extends StatelessWidget {
  const SnackbarError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
